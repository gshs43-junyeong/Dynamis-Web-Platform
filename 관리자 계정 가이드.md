# 관리자 계정 가이드

Dynamis Web Platform 관리자(admin) 계정의 권한 범위와 관리자 콘솔 사용법, 그리고 관리자 계정을 승격/회수하는 절차를 안내합니다.

작성 기준: 2026-08-06 `firebase.rules` 최종 수정본 기준

## 1. 관리자 권한은 `role` 필드 하나로 결정됩니다

**Firestore `users/{uid}` 문서의 `role` 필드가 유일한 권한 판정 기준입니다.**

보안 규칙은 요청자 본인의 users 문서를 직접 읽어서 판정합니다.

```js
function selfRole() {
  return selfExists() ? selfDoc().role : 'none';
}
function isAdmin() {
  return selfRole() == 'admin';   // ← 이것이 전부
}
```

> 🚨 **`role` 변경은 화면 표시만 바꾸는 것이 아니라, 그 자리에서 실제 서버 권한을 부여하는 행위입니다.**
> `/admin` 콘솔에서 어떤 계정의 등급을 `admin`으로 바꾸는 순간 그 계정은 전 회원 문서 수정·삭제, 모든 글 수정·삭제, 일일 한도 면제, 첨부 용량 정책 면제 권한을 즉시 갖게 됩니다. "일단 바꿔 보고 아니면 되돌리자"는 식으로 다루지 마세요.

**Firebase Auth 커스텀 클레임(`admin: true`)은 현재 이 프로젝트에서 사용하지 않습니다.** `firebase.rules` 어디에서도 `request.auth.token`을 참조하지 않으므로, 클레임을 심어도 아무 효과가 없고 지워도 권한이 회수되지 않습니다.

> 📌 이력: 한때 `get()`이 동작하지 않는 것으로 보여 커스텀 클레임 방식으로 전환했다는 기록이 이 문서에 있었으나, 이후 규칙이 `get()` 기반으로 되돌아오면서 그 서술은 무효가 되었습니다(문서만 남아 있었음). 지금은 `get()` 기반 `role` 판정으로 통일되어 있으며, 승격/회수 절차에 별도 스크립트나 서비스 계정 키가 필요하지 않습니다. (2026-08-06 정정)

일반 가입은 항상 `role: 'general'`로 생성되며, 규칙이 create 시 `role == 'general'`을 강제하므로 본인이 스스로 관리자로 승격할 수 있는 경로는 없습니다. 등급 변경은 이미 `role: 'admin'`인 계정만 수행할 수 있습니다.

## 2. 새 계정을 관리자로 승격시키는 방법

1. 대상 계정이 이미 Dynamis에 회원가입되어 있어야 합니다 (Google/GitHub 로그인으로 정상 가입 완료 상태).
2. Dynamis 사이트 → `/admin` → 대상 유저의 등급을 **"관리자(admin)"** 로 변경 → **변경 확정**.
3. 끝입니다. 서버 권한은 **즉시** 반영됩니다 (규칙이 매 요청마다 users 문서를 읽으므로).
4. 대상 계정 화면에 관리자 메뉴가 보이려면 **새로고침 또는 재로그인**이 필요할 수 있습니다. 이는 클라이언트가 로그인 시점의 사용자 정보를 캐시하기 때문이며, 권한 자체는 이미 부여된 상태입니다.
5. 관리자 콘솔에서 기능(경고 부여 / 등급 변경 / 계정 삭제)이 정상 작동하는지 테스트합니다.

Firebase 콘솔에서 직접 하려면: Firestore Database → `users` 컬렉션 → 대상 문서 → `role` 필드를 `admin`으로 편집해도 동일합니다.

## 3. 관리자 권한을 회수(강등)하는 방법

1. `/admin` 콘솔에서 대상 `role`을 `general`/`member` 등으로 변경합니다.
2. 끝입니다. 이 시점에 서버 권한도 함께 회수됩니다.
3. 대상이 이미 로그인 중이었다면 화면상 관리자 메뉴는 새로고침 전까지 남아 있을 수 있으나, 버튼을 눌러도 서버가 거부합니다.

> ⚠️ 강등 후에는 `memberProfiles/{uid}`의 공개 등급 표시도 함께 갱신되어야 합니다. 관리자 콘솔이 자동으로 동기화하지만 실패할 수 있으므로, 강등 직후 부원 목록에서 해당 인원의 등급 표기가 실제로 바뀌었는지 눈으로 확인하세요.

## 4. ⚠️ 절대 스스로를 잠그지 마세요 (Lockout 방지)

관리자가 단 1명뿐인 상태에서 그 계정의 `role`을 실수로 낮추면, 이후 누구도 관리자 콘솔에서 admin을 다시 부여할 수 없게 됩니다 (관리자만 등급변경을 할 수 있으므로).

복구 방법: **Firebase 콘솔 → Firestore Database → `users` 컬렉션 → 해당 문서의 `role`을 `admin`으로 직접 편집**합니다. 콘솔 접근 권한만 있으면 되고, 서비스 계정 키나 별도 스크립트는 필요하지 않습니다.

가능하면 관리자 계정을 2개 이상 유지해 이 상황 자체를 피하세요.

## 5. 관리자 콘솔 기능

관리자로 로그인하면 상단 네비게이션에 **관리자(Admin)** 메뉴가 나타납니다.

### 등급 변경
회원 목록에서 등급(`general`/`honored`/`member`/`admin`)을 드롭다운으로 선택하고 **변경 확정**을 누르면 즉시 반영됩니다.
- `general`: 등급 없음 (가입 직후 기본값)
- `honored`: 명예부원 (공지/이벤트 작성 가능, 파일 첨부 불가)
- `member`: 부원 (공지/이벤트 작성 및 파일 첨부 가능)
- `admin`: 관리자 (모든 권한 + 관리자 콘솔 접근). **선택 즉시 실제 서버 권한이 부여됩니다** — 1절 경고 참고.

### 경고 조치
**경고 조치** 버튼을 누르면 해당 계정의 누적 경고 횟수가 1회 증가하고, 다음 로그인 시 경고 알림이 표시됩니다. 경고 누적에 따른 자동 정지/삭제 처리는 없으며, 계정 강제 탈퇴 여부는 관리자가 직접 판단해 별도로 수행합니다.

### 계정 삭제 (강제 탈퇴)
**계정 삭제** 버튼은 대상 계정을 강제 탈퇴시킵니다.
- 대상이 작성한 공지사항/이벤트/댓글을 best-effort로 함께 정리합니다 (일부 정리가 실패해도 계정 삭제 자체는 진행됩니다).
- 대상의 일일 트래픽 통계는 규칙상 본인만 읽고 쓸 수 있어 관리자가 지울 수 없습니다.
- 경고 이력은 대상이 본인 계정 문서를 삭제 후 재가입하면 초기화됩니다. 반복 제재가 필요한 상대라면 이 점을 감안하세요 (구조적 해결은 별도 제재 이력 컬렉션 분리가 필요).
- 대상의 Firebase Auth 로그인 계정 자체는 관리자가 삭제할 수 없습니다. Firestore 상의 계정 정보만 삭제되므로, 그 사람이 재로그인을 시도하면 "가입되지 않은 계정"으로 처리되어 재가입이 필요합니다.

### 공지사항 고정
공지사항 목록에서 관리자에게만 보이는 **고정** 체크박스로 특정 공지를 상단에 고정할 수 있습니다.

## 6. 보안 유의사항

1. 관리자 등급 부여는 되돌리기 어려운 조치처럼 다루세요.
   - `role: 'admin'`은 부여 즉시 서버 권한이며, 그 계정이 탈취되면 전 회원 정보 열람·수정이 가능해집니다.
   - 관리자 계정의 Google/GitHub 계정에는 반드시 2단계 인증을 설정하세요. 이 프로젝트는 소셜 로그인만 쓰므로, 그쪽 계정 보안이 곧 관리자 계정 보안입니다.
2. `serviceAccountKey.json`을 발급했다면 Firebase 프로젝트 전체에 대한 관리자급 비밀 키입니다.
   - 절대 GitHub 등 공개 저장소에 커밋하지 말 것 (`.gitignore`에 이미 포함됨).
   - 현재 운영 절차상 이 키는 **필요하지 않습니다**. 과거 커스텀 클레임 스크립트용으로만 쓰였고 그 방식은 폐기되었으므로, 남아 있는 키가 있다면 폐기하는 편이 안전합니다.
3. 배포 파일명에 주의하세요.
   - 이 프로젝트는 `firebase.json`이 `firestore.rules`가 아니라 `firebase.rules`라는 파일명을 가리키고 있습니다.
   - 두 이름의 파일이 동시에 존재하면 배포가 엉뚱한 파일을 대상으로 이루어져 수정사항이 반영 안 되는 것처럼 보이니, 항상 `firebase.json`의 `firestore` → `rules` 값을 먼저 확인하고 그 경로의 파일만 수정할 것.
4. 규칙 배포 후 콘솔 로그에 `[W]` 경고가 없는지 확인하세요. 경고가 있어도 "Deploy complete!"는 뜨지만, 그 경고가 걸린 함수(예: 이전의 `startsWith`)는 실제로는 오작동할 수 있습니다.
5. 관리자 권한 남용(사적 목적의 정보 열람/유출 등)은 커뮤니티 이용 가이드라인 제4조에 의해 관리자 역할 박탈 및 계정 삭제 대상이 됩니다.

## 7. 신규 관리자 승격 시 체크리스트 (요약)

- [ ] 1. 대상 계정 가입 완료 확인
- [ ] 2. 대상이 정말 관리자 권한을 가져야 하는 사람인지 확인 (부여 즉시 실권한)
- [ ] 3. 대상 계정의 Google/GitHub 2단계 인증 설정 여부 확인
- [ ] 4. 관리자 콘솔에서 `role` → `admin` 변경 후 **변경 확정**
- [ ] 5. 대상 계정 새로고침 또는 재로그인 (화면 반영용)
- [ ] 6. 관리자 기능(경고/등급변경/삭제) 실제 작동 테스트
- [ ] 7. 관리자 계정이 2개 이상 유지되는지 확인 (lockout 방지)
